<Window x:Class="ClashManager.AutoNaming.Views.ParameterSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Настройки параметра" Height="230" Width="350" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Настройки параметра" FontSize="14" FontWeight="Bold" Margin="0,0,0,15" />

        <!-- Настройки -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Разделитель внутри параметра:" Margin="0,0,0,5" />
            <TextBox x:Name="ParameterSeparatorTextBox" Text="," Width="100" HorizontalAlignment="Left" />
            <TextBlock Text="По умолчанию выводятся все значения параметра всех элементов коллизии" FontSize="10" Foreground="Gray" Margin="0,5,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="OK" Width="80" Click="OkButton_Click" IsDefault="True" />
            <Button x:Name="CancelButton" Content="Отмена" Width="80" Margin="10,0,0,0" Click="CancelButton_Click" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
