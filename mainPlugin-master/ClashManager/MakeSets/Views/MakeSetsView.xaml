<Window x:Class="ClashManager.MakeSets.View.MakeSetsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Height="550" Width="450"
        Title="Наборы по параметрам"
        WindowStartupLocation="CenterScreen">
    <StackPanel Margin="10">
        <TextBlock Text="Путь к CSV файлу:" Margin="0,0,0,5"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding CsvFilePath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" Margin="0,0,5,0"/>
            <Button Grid.Column="1" Content="Обзор..." Command="{Binding BrowseCsvCommand}"/>
        </Grid>
        <Button Content="Создать наборы" Command="{Binding CreateSetsCommand}" Margin="0,10,0,0"/>
    </StackPanel>
</Window>