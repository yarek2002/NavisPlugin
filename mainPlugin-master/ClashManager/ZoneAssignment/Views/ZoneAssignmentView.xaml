<Window x:Class="ClashManager.ZoneAssignment.Views.ZoneAssignmentView"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		mc:Ignorable="d"
		Title="Назначение зон" Height="400" Width="600" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Назначение зон коллизиям" FontSize="16" FontWeight="Bold" Margin="10" />

        <!-- Основная область -->
        <GroupBox Header="Выберите NWC файл с зонами" Grid.Row="1" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Доступные NWC файлы:" Margin="5,5,5,0" />
                <ListBox x:Name="NwcFilesListBox" Grid.Row="1" Margin="5"
                         SelectionMode="Single"
                         DisplayMemberPath="DisplayName">
                </ListBox>
            </Grid>
        </GroupBox>

        <!-- Информация -->
        <GroupBox Header="Информация" Grid.Row="2" Margin="10,0,10,10">
            <StackPanel Margin="10">
                <TextBlock Text="Автоматическое назначение зон:" FontWeight="Bold" Margin="0,0,0,5" />
                <TextBlock Text="• Выберите NWC файл, содержащий зоны (коробки) из Revit и нажмите Зонировать" TextWrapping="Wrap" Margin="0,0,0,3" />
                <TextBlock Text="• Нажмите Авто-группировка по зонам" Margin="0,0,0,3" />
                <TextBlock Text="• Все коллизии внутри зон получат названия, соответсвующие комментарию формы (зоны)" Margin="0,0,0,3" />
            </StackPanel>
        </GroupBox>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button x:Name="AssignZonesButton" Content="Зонировать" Width="120" Margin="0,0,10,0" Click="AssignZonesButton_Click" />
            <Button x:Name="CancelButton" Content="Отмена" Width="80" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
